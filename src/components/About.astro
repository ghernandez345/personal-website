---
import { Image } from "astro:assets";
import SectionContainer from "./SectionContainer.astro";
import myImage from "../assets/img/me.jpg";
---

<SectionContainer class="about-container">
  <Image
    src={myImage}
    priority
    alt="Gabriel Hernandez wearing a nice green shirt and smiling friendly"
  />
  <div class="about-text">
    <h2>About Me</h2>
    <p>
      I'm a software developer who enjoys building cool applications.
    </p>
    <ul>
      <li>
        <a href="https://www.linkedin.com/in/gabriel-hernandez-gh" target="_blank">Linkedin</a>
      </li>
      <li>
        <a href="https://www.github.com/ghernandez345" target="_blank">Github</a>
      </li>
    </ul>
  </div>
</SectionContainer>

<style>
  .about-container {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xl, 24px);
  }

  img {
    max-width: 256px;
    width: 100%;
    height: 100%;
    border-radius: var(--border-radius-l);
  }

  .about-text {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xl);
  }

  h2 {
    color: var(--color-text-primary);
    font-family: var(--font-family-outfit);
    font-size: var(--font-size-48);
    font-style: normal;
    font-weight: var(--font-weight-semi-bold);
    line-height: 140%; /* 67.2px */
    letter-spacing: var(--font-letter-spacing-07);
  }

  p {
    color: var(--color-text-secondary);
    font-family: var(--font-family-public-sans);
    font-size: var(--font-size-24);
    font-style: normal;
    font-weight: var(--font-weight-regular);
    line-height: 140%; /* 33.6px */
    letter-spacing: var(--font-letter-spacing-03);
  }

  ul {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-l);
  }

  ul a {
    color: var(--color-text-brand);
    font-family: var(--font-family-outfit);
    font-size: var(--font-size-32);
    font-style: normal;
    font-weight: var(--font-weight-regular);
    line-height: 140%; /* 44.8px */
    letter-spacing: var(--font-letter-spacing-02);
  }

  a:hover {
    text-decoration: underline;
  }

  @media (min-width: 480px) {
    img {
      max-width: 400px;
    }
  }

  @media (min-width: 768px) {
    .about-container {
      flex-direction: row-reverse;
      padding: var(--spacing-xh) 0;
    } 

    img {
      max-width: 289px;
    }
  }
  
  @media (min-width: 1024px) {
    img {
      max-width: 492px;
    }
  }

  @media (min-width: 1280px) {
    .about-container {
      justify-content: space-between;
      padding: var(--spacing-xxh) 0;
    }

    img {
      max-width: 513px;
    }
  }
</style>
